spring.application.name=backend

# Database Configuration (uses environment variables)

# MySQL Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/bookfast_db?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.hbm2ddl.auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.properties.hibernate.physical_naming_strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

# Server Configuration
server.port=${PORT:8080}

# CORS Configuration - Production deployment
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}

# Frontend URL (for emails)
app.frontend.url=${FRONTEND_URL:http://localhost:4200}

# Cookie Configuration
# For production: Set COOKIE_DOMAIN to your domain (e.g., .vercel.app)
# For localhost: Leave empty or set to localhost
cookie.domain=${COOKIE_DOMAIN:}
cookie.secure=${COOKIE_SECURE:false}

# SendGrid Email API Configuration
# REQUIRED for production: Set SENDGRID_API_KEY environment variable
# For local development: Set environment variable
# To set environment variable: set SENDGRID_API_KEY=your-api-key
sendgrid.api-key=${SENDGRID_API_KEY:}
sendgrid.sender.email=${SENDGRID_SENDER_EMAIL:thakarekunaljb@gmail.com}
sendgrid.sender.name=${SENDGRID_SENDER_NAME:BookFast}

# Twilio SMS API Configuration
twilio.account.sid=${TWILIO_ACCOUNT_SID:}
twilio.auth.token=${TWILIO_AUTH_TOKEN:}
twilio.phone.number=${TWILIO_PHONE_NUMBER:}

# JWT Configuration
jwt.secret=${JWT_SECRET:mySecretKey123456789012345678901234567890123456789012345678901234567890}

# Admin Configuration
admin.default.email=${ADMIN_EMAIL:admin@bookfast.com}
admin.default.password=${ADMIN_PASSWORD:admin123}
admin.default.firstname=${ADMIN_FIRSTNAME:System}
admin.default.lastname=${ADMIN_LASTNAME:Administrator}
admin.auto-create=${ADMIN_AUTO_CREATE:true}

# Google Calendar API Configuration
google.calendar.client.id=${GOOGLE_CALENDAR_CLIENT_ID:}
google.calendar.client.secret=${GOOGLE_CALENDAR_CLIENT_SECRET:}
google.calendar.redirect.uri=${GOOGLE_CALENDAR_REDIRECT_URI:${app.frontend.url}}
google.calendar.scope=https://www.googleapis.com/auth/calendar
google.calendar.auth.uri=https://accounts.google.com/o/oauth2/auth
google.calendar.token.uri=https://oauth2.googleapis.com/token

# Imgur API Configuration (for profile picture uploads)
imgur.client.id=${IMGUR_CLIENT_ID:546b2e1e7b1b1e7}
